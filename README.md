# Telegram-Ingestion-Pipeline
A robust pipeline to extract text, media, events, and metadata from Telegram group chats and store them in a vector database, enabling retrieval-augmented generation (RAG) for LLMs based on group chat data.
